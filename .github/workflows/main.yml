name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Run a one-line script
      run: echo Hello, world!
    - name: get Stata
      run: curl -u tera_aimaomao:tera_aimaomao https://kita.teracloud.jp:443/dav/Stata/Stata16Linux64-Legacy.tar.gz -o stata.tar.gz
    - name: install Stata
      run: |
        tar -xzf stata.tar.gz
        cd stata
        pwd
        ls
        
      
